# Optimizing Speed with Cython
## Setup and Build
```
$ ./build.sh
```

## Activate Virtual Environment
```
$ source .virtualenv/bin/activate;
```

## Run
Same as `identify_writer.py` in the root dir.
```
$ python3 run.py
```

use `--help` to list available options